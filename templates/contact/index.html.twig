{% extends 'base.html.twig' %}

{% block title %}Contact index{% endblock %}

{% block body %}
<section>

    <div class="container" style="margin-top: 10%;" >
    <div class="row row- justify-content-center" >
        <div class="col-8"> 
        <nav class="navbar navbar-expand-lg fixed-top">
    <div class="container">

        <a class="navbar-brand" href="http://127.0.0.1:8000">Titan Club Gym</a>

        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false"
                aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ml-lg-auto">
                <li class="nav-item">
                    <a href="http://127.0.0.1:8000" class="nav-link smoothScroll">Home</a>
                </li>

                <li class="nav-item">
                    <a href="http://127.0.0.1:8000" class="nav-link smoothScroll">Notre Centre</a>
                </li>

                <li class="nav-item">
                    <a href="http://127.0.0.1:8000" class="nav-link smoothScroll">Nos Packs</a>
                </li>

                <li class="nav-item">
                    <a href="http://127.0.0.1:8000" class="nav-link smoothScroll">Planning Des Cours</a>
                </li>

                <li class="nav-item">
                    <a href="http://127.0.0.1:8000" class="nav-link smoothScroll">Contact</a>
                </li>

            </ul>

            <ul class="social-icon ml-lg-3">
                <li><a href="https://www.facebook.com/Titan-club-gym-100684435081842" class="fa fa-facebook"></a></li>

            </ul>

        </div>

    </div>
</nav>
    <h1>Messages des Clients</h1>

    <table class="table table-striped table-dark" >
        <thead>
            <tr>
                <th>Id</th>
                <th>Email</th>
                <th>Tel</th>
                <th>Message</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for contact in contacts %}
            <tr>
                <td>{{ contact.id }}</td>
                <td>{{ contact.email }}</td>
                <td>{{ contact.tel }}</td>
                <td>{{ contact.message }}</td>
                <td>
                    <a href="{{ path('contact_show', {'id': contact.id}) }}" class="btn btn-lg btn-success"  
                     style="background: #28a74557" type="submit">Show</a>
                     <p> </p>
                    <a href="{{ path('contact_edit', {'id': contact.id}) }}" class="btn btn-lg btn-success"  
    style="background: #28a74557" type="submit" >Edit</a> 
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

  
      </div>       
        </div>
          </div>
</section>
{% endblock %}
